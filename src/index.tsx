import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

const rootElement = document.getElementById("root");
console.log("pr을 날려보자");
console.log("pr을 날려보자 수정을했다.");

ReactDOM.render(<App />, rootElement);
